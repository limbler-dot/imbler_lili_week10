# SETUP: I told Codex to create and use a virtual environment called .venv, then activate it for windows, install streamlit / requests, add these to requirements.txt, and run the app. It experienced difficulty opening the Streamlit website so I had to do so manually. 
# It created a plan to create and activate the virtual environment while installing the necessary packages. The packages were added to requirements.txt. 
# I implemented what it gave after extensive back and forth. 
